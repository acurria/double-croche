export const metadata = {
	title: 'Musique - Playlists',
	description: 'A faire',
}

export default function Layout({
								   children,
							   }: {
	children: React.ReactNode
}) {
	return (
		<>{children}</>
	)
}