export const metadata = {
	title: 'Musique - Festivals',
	description: 'A faire',
}

export default function Layout({
								   children,
							   }: {
	children: React.ReactNode
}) {
	return (
		<>{children}</>
	)
}