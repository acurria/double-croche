export const metadata = {
	title: 'Cinéma - Chroniques',
	description: 'A faire',
}

export default function Layout({
								   children,
							   }: {
	children: React.ReactNode
}) {
	return (
		<>{children}</>
	)
}