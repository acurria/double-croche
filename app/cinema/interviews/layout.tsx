export const metadata = {
	title: 'Musique - Interviews',
	description: 'A faire',
}

export default function Layout({
								   children,
							   }: {
	children: React.ReactNode
}) {
	return (
		<>{children}</>
	)
}